import React, { Component } from 'react';

import Container from '../../ui/Container';

export default class Settings extends Component {
  render () {
    return (
      <Container>
        <div>application settings such as full node overview, secure signer config, etc.</div>
      </Container>
    );
  }
}
