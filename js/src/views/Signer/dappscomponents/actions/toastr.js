
import { createAction } from 'redux-actions';

export const addToast = createAction('add toast');
export const removeToast = createAction('remove toast');
export const onClickToast = createAction('on clickToast');
