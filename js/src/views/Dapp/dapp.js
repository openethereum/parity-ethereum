import React, { Component } from 'react';

import Container from '../../ui/Container';

export default class Dapp extends Component {
  render () {
    return (
      <Container>
        <div>managing and interacting a single contract will be displayed here</div>
      </Container>
    );
  }
}
