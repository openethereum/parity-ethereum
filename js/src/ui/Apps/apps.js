import React, { Component } from 'react';

export default class Apps extends Component {
  render () {
    return (
      <h2>managing contracts (adding, calling, etc) should go in here, next steps...</h2>
    );
  }
}
