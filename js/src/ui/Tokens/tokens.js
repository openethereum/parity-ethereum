import React, { Component } from 'react';

export default class Tokens extends Component {
  render () {
    return (
      <h2>the token dapp interface should go in here, next steps...</h2>
    );
  }
}
