[package]
description = "Parity Ethereum Common Types"
name = "common-types"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]

[dependencies]
ethereum-types = "0.6.0"
ethjson = { path = "../../json" }
parity-util-mem = "0.1"
ethkey = { path = "../../accounts/ethkey" }
keccak-hash = "0.2.0"
parity-bytes = "0.1"
rlp = "0.4.0"
rlp_derive = { path = "../../util/rlp-derive" }
unexpected = { path = "../../util/unexpected" }
ethbloom = "0.6"
derive_more = "0.15.0"
rustc-hex = "2.0.1" # todo: only needed for CommonParams. Remove.
vm = { path = "../vm"} # todo: this is irritating

[dev-dependencies]
rustc-hex= "1.0"
